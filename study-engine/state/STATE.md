# 기술 백과사전 엔진 — 상태

**엔진은 실행 사이에 기억이 없다.** 다음 사이클이 이어받을 것은 이 파일뿐이다.

## 지금 쓰는 중

**직전 사이클에서 `옴의 법칙`(`ohms-law`)을 끝냈다** — 절 5 · 표 3 · 실습 3 · 지뢰 6 ·
근거 5. 전체: 문서 8개(전부 done) · 절 43 · 실습 24 · **오류 0 · 경고 2.**

**⚠️ 미결 경고 2건 — `python.json` 의 `market` 이 낡았다.** `tech_relations.json` 이
재생성돼 숫자가 바뀌었다(공고 **2,648 → 2,594**, **35.4% → 35.0%**). 사다리에서는
재방문이 5순위라 대기열이 있는 동안 안 걸린다. **대기열이 비거나 `fix(study)` 사이클을
돌 때 한 번에 처리한다** — 그때 `market.postings`·`pct_jobs`·`as_of` 와 `with[].pct`
를 그 시점 파일에서 다시 옮긴다. **이 데이터는 매 크롤 사이클마다 바뀌므로 앞으로도
계속 뜰 경고다** — 문서가 늘면 `market` 있는 문서를 한 사이클에 몰아 갱신하는 편이 낫다.

**다음 사이클 = QUEUE 맨 위 `인터럽트`(`interrupt`)** — 사다리 3순위(신규).
**두 문서가 가리키는 끊긴 링크**다(`uart` 의 오버런 절이 "본체는 여기"라며 넘겼고,
`atmega128` 은 INT0~INT7 로 가리킨다).

**쓸 때 지킬 것 — 앞 문서들이 이미 사례를 만들어 뒀다.**
- **사례를 새로 만들지 말고 이어 쓴다.** `uart` 의 수신 오버런(115200bps = 87µs 마다
  한 바이트)과 `gpio-register` 의 read-modify-write 경쟁이 이 문서가 설명할 두 사례다.
- 축은 **폴링과의 대비** — 언제 인터럽트가 이기고 언제 폴링이 나은가(`when_to_use`).
- ISR 안에서 하면 안 되는 것(긴 작업·`printf`·블로킹), `volatile` 이 왜 필요한가,
  공유 변수의 원자성(16비트 변수를 8비트 MCU 에서 읽는 문제), 인터럽트 지연·중첩.
- 1차 자료: ATmega128 데이터시트의 인터럽트 벡터 표와 SREG/I 비트, avr-libc 의
  `<avr/interrupt.h>`·`ATOMIC_BLOCK` 문서(URL 은 `WebSearch` 로 확인하고 건다).
- 실습은 장비 없이: **경쟁 상태를 PC 스레드로 재현**(`gpio-register` 의 두 번째 실습을
  변형), 그리고 ISR 이 놓치는 조건을 계산으로 확인(바이트 도착 간격 vs ISR 실행 시간).

## 배운 것

- **문서끼리 축을 나눈다.** 같은 주제를 두 문서가 다루면 반드시 겹치므로, **먼저 쓴
  문서에 무엇이 있는지 확인하고 링크로 넘긴다.** `i2c` 는 풀업 계산을, `ohms-law` 는
  풀업 값 고르기를 각각 `pull-up-resistor` 로 넘겼다.
- **작은 낱말은 "왜 지금까지 미뤘나"가 곧 축이다.** `ohms-law` 는 공식이 아니라
  *계산값 vs 살 수 있는 값(E24)*, *±5% 오차*, *정격 전력* 셋으로 썼다. 공식 소개는
  세상에 넘치고, 이 셋이 실무에서 막히는 자리다.
- **개념 문서는 대응표가 본체다**(`gpio-register` 의 다섯 질문, `i2c` 의 증상→원인 표).
- **PDF 1차 자료는 URL·신원까지만 확인되는 경우가 많다**(ESP32 TRM, UM10204 둘 다 본문
  추출 실패). **확인한 만큼만 쓰고 못 한 것은 `open_questions` 에 적는다.**
- **유료 표준이 반복해서 걸린다** — IEC 60062(색띠), IEC 60063(E24), TIA/EIA-485.
  링크를 지어내지 않고 **제조사 데이터시트를 대신 인용**하는 것이 이제 정해진 처리다.
- **웹 검색은 제도·표준·벤더 문서 URL 확인에서 값을 한다**(금융위 보도자료, ST 매뉴얼,
  Vishay 데이터시트).
- **alias 는 자원이다.** 도메인 문서가 언어 이름을 가져가면 언어 문서가 못 걸린다.
- **`market` 이 있는 문서는 동반 기술이 목차를 정해 준다**(Python 의 AWS·Docker·K8s).
  **대신 그 숫자는 크롤마다 바뀌어 경고로 남는다**(위 미결 항목).
- **낱말이 공고에 안 걸리면 그 낱말이 전제되는 문장을 찾는다.** `옴의 법칙` 도 0건이었고
  `전류측정 Tool`·`전력 소모 효율화`·`발열, 전력소모`·`Analog 회로 설계` 로 받쳤다.
- **한 문서가 다음 큐를 만든다.** 끊긴 링크로 큐에 오른 것: `i2c`·`ohms-law`(풀업),
  `interrupt`·`rs485`(UART), `gil`·`asyncio`(Python), `java`(금융 Java), `stm32`(GPIO),
  `spi`(I²C).

## 재시도 안 함

| 무엇 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
