# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| Git | `git` | ⭐ **`state-management` 가 끊긴 링크를 안 남겨 층을 바꾼 자리**(프론트엔드 → 협업/도구). **이 층은 통째로 비어 있다.** ⭐⭐ **셈(2026-09-06): `Git|버전 관리` 3086건(모집중 963)** · `코드 리뷰|Pull Request` **1634** · `rebase|리베이스` **230** · `브랜치 전략|Git Flow` 129 · ⚠️ `머지 충돌|conflict` **14** · ⚠️⚠️ `커밋 컨벤션|커밋 메시지` **4건(모집중 0)**. ⚠️ **`aliases` 확인 완료**: `Git`·`깃`·`rebase`·`브랜치`·`머지`·`코드 리뷰`·`버전 관리` 어느 것도 없다. ⚠️⚠️ **3086건짜리 낱말이라 그대로 쓰면 사용법 요약본이 된다.** ⭐⭐⭐ **축을 좁힌다: "이미 나간 것은 못 고친다."** ⭐⭐ **이 백과사전이 계속 만난 주제의 가장 일상적인 형태**다 — `api-versioning`(이미 남이 쓰는 API), `android`/`ios`(이미 깔린 앱), `javascript`(이미 배포된 웹). **여기는 이미 push 한 커밋**이고, 하루에 몇 번씩 만난다. ⭐⭐⭐ **1차 자료가 그 축을 직접 준다** — `git rebase` 매뉴얼에 **`RECOVERING FROM UPSTREAM REBASE`** 라는 절이 통째로 있고, **"...는 나쁜 생각이다"** 부류의 경고가 붙어 있다. ⭐ `javascript`("would be removed")·`go`("Why does Go not have X?")에 이은 계보이면서 형태가 또 다르다: **도구가 자기 기능의 사고 복구 절차를 매뉴얼에 적어 둔다.** 축 넷: ①⭐⭐**커밋은 불변이고 브랜치는 포인터다**(그래서 '고친다'가 실은 '새로 만들고 가리키는 것을 옮긴다') ②⭐⭐⭐**남이 그 위에 쌓았으면 못 고친다**(rebase 경고 — `230건`) ③**코드 리뷰가 진짜 큰 낱말**(**1634건**, Git 다음으로 큼 — ⚠️ 도구가 아니라 **협업의 계약**) ④⚠️**커밋 메시지 4건**(⭐ 이 저장소의 `CLAUDE.md` 가 Conventional Commits 를 규칙으로 쓴다 — **자기 레포를 1차 자료로 쓴 `scraping`·`seo` 사이클처럼 쓸 수 있다**). **이웃**: `github-actions`(⚠️ **축 확인 필수** — 그쪽은 CI 보안) · `api-versioning` · `monorepo` · `code-review`?(있는지 확인). | ⭐⭐⭐ **1차 자료: `git rebase` 공식 매뉴얼의 `RECOVERING FROM UPSTREAM REBASE` 절** — **도구가 자기 기능이 만드는 사고와 복구법을 스스로 적는 자리**. 보조로 `git commit`/`git reset` 매뉴얼. ⭐ **부정어 먼저**: bad idea · do not · never · dangerous · lost · rewrite · will not · avoid. ⚠️ **git-scm.com 은 페이지가 크다** — 못 열면 밝히고 2차 경로를 쓴다(`javascript` 사이클 규칙) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
