# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| REST API | `rest` | ⭐ **후보 조사(사다리 6순위)로 찾았다** — `authz` 가 끊긴 링크를 하나도 안 남겨 큐가 비었고, `tech_relations.json` 의 60개 중 **문서 없는 48개**를 공고 수로 줄 세워 골랐다. ⭐⭐⭐ **셈 대비가 이 백과사전 최대다: `REST` 3372건(모집중 1157) vs `HATEOAS` 0건.** `RESTful` 1313 · `API 설계` 1691 · `GraphQL` 370 · `gRPC` 243. **REST 를 만든 사람이 필수 제약이라고 못 박은 것(하이퍼미디어/HATEOAS)이 3372건 중 0건**이라는 뜻이고, `security-certification`(ISMS 582 vs FIPS140 1)보다 극단적이다. ⭐⭐⭐ **그래서 축이 이미 서 있다: "모두가 REST 라고 부르는데 창시자는 그건 REST 가 아니라고 적었다 — 그리고 셈이 그것을 증명한다."** 이 백과사전이 반복해 온 **"제품이 스스로 그은 선"** 의 가장 강한 형태(만든 사람이 자기 이름의 오용을 직접 반박한다). 근거는 흔하다: 디스펙터 `**RESTful API 설계 및 구현** 경험`, 액션파워 `**RESTful API 설계** 및 DB 모델링, 캐시/트랜잭션 관리 등 **서버 기본기**`(⭐ 기본기로 분류된다), 큐엠아이티 `사용성 높은 **API(REST/GraphQL)** 설계`, 동선컴퍼니 `효율적인 데이터 구조 설계를 위한 **RESTful API 디자인**`. ⚠️⚠️ **경계 — 이웃이 셋 있다**: `api-versioning`(바꾸는 문제), `openapi`(문서·계약), `contract-testing`(검사). **이 문서는 그 셋의 아래에 서는 기본편** — "REST 가 무엇이고, 왜 다들 다르게 쓰나". 축: ①⭐⭐**이름과 실물의 간극**(3372 vs 0) ②**여섯 제약과 그중 실제로 지켜지는 것** ③⭐**성숙도 모델**(Richardson — 0단계 RPC → 1 자원 → 2 HTTP 동사 → 3 하이퍼미디어; **대부분 2단계에서 멈춘다**) ④**그래도 2단계가 나쁜가**(⭐ 실용의 편에서 — 왜 멈추는 것이 합리적인지도 적어야 공정하다) ⑤**언제 REST 대신 GraphQL·gRPC 인가**(370·243건 — `when_to_use` 의 `over` 가 저절로 채워진다) | ⭐ **1차 자료** — **Roy Fielding 의 박사학위 논문 5장**(REST 의 정의와 여섯 제약, uci.edu 에 공개 HTML) · ⭐⭐ **Fielding 의 블로그 "REST APIs must be hypertext-driven"**(2008 — **"내가 보는 것 중 REST 라고 불리는 것 대부분은 REST 가 아니다"** 취지의 글, `roy.gbiv.com`) · **Richardson Maturity Model** 설명 문서. ⚠️ 셋 다 열리는 편이지만 **블로그는 오래된 사이트**라 안 되면 논문 5장만으로도 축이 선다 |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
