# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| 프로파일링 | `profiling` | ⭐⭐⭐ **`performance` 가 방금 `open_questions` 2번에 "이 문서 다음의 가장 큰 빈자리"라고 적었다** — **세 사이클 연속 문서가 스스로 지목**한 자리다(`microservices`→`bounded-context`→`hexagonal-architecture`, 그리고 `performance`→여기). ⭐⭐ **셈(2026-09-06): `프로파일링|프로파일러|profiling|profiler` 180건(모집중 105)** — ⚠️ **작다.** 다만 `헥사고날` 22건·`DDD` 88건보다 크고, ⭐ **`성능 최적화` 2976건이 전제하는 단계**라 자리가 분명하다. ⭐⭐⭐ **축 후보: "자원 단위에서 안 잡히면 코드 단위로 내려간다 — 그런데 그 단계를 이름으로 부르는 곳이 180건뿐이다."** 앞 문서가 세운 순서(질문 → 자원 → 세 지표)의 **그다음 칸**이다. ⚠️⚠️ **우려 둘을 먼저 적어 둔다.** ①**같은 저자를 두 사이클 연속 쓰게 된다**(Brendan Gregg) ②**같은 층(성능)이 두 사이클 연속**이다. ⭐ **그래도 사다리 2순위(문서가 스스로 지목)를 우선한다** — 지난 세 번 다 그렇게 했고 결과가 좋았다. ⭐⭐ **1차 자료: `Flame Graphs`**(brendangregg.com/flamegraphs.html) — ⭐ **그가 만든 표현 방식**이고 **언어와 무관**해서 1차급으로 쓸 수 있다(언어별 도구는 py-spy·async-profiler·perf 로 갈린다). 축 넷: ①⭐⭐⭐**왜 그림이 필요한가**(스택 트레이스를 눈으로 못 읽는다 — 원문이 그 문제를 어떻게 적는지) ②⭐⭐**무엇을 읽는가**(폭 = 시간, 높이 = 스택 깊이 — ⚠️ **가로축이 시간 순서가 아니라는 것**이 가장 흔한 오독일 수 있다. **원문 확인**) ③**샘플링 대 계측**(overhead) ④⚠️**운영 중에 뜰 수 있나**. ⚠️ **겹침 확인 필수**: `performance`(그쪽은 자원 단위까지만) · `gc`(p99 8회) · `load-test`(병목 20회) · `observability`. ⚠️ **aliases 확인 필요**(`프로파일링`·`플레임 그래프`·`샘플링`). ⭐⭐⭐ **1차 자료를 여는 그 호출에 실패 사례 검색을 함께 넣는다** — 네 사이클 연속 지켰다(⚠️ 넷 다 2차 자료뿐이었고 그 사실을 매번 기록했다). | ⭐⭐⭐ **1차 자료: brendangregg.com/flamegraphs.html**. ⭐ **부정어 먼저**: not · doesn't · isn't · without · misread · confusing · can't. ⚠️ **못 열면 축을 바꾼다** — 대안: `애자일\|스크럼\|스프린트` **662건**(⭐ 층이 완전히 다르고 1차 자료가 **agilemanifesto.org** 로 아주 확실하다 — **네 줄짜리 선언문인데 그 네 줄을 부르는 공고가 몇이나 되는지 세면 축이 나온다**) 또는 `온보딩` **749건**(`documentation` 이 남긴 자리) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ 테스트 피라미드 (`testing` 보강) | **`testing` 이 "가장 큰 빈자리"라고 적었으나 셈이 없다** — `테스트 피라미드` **2건**. ⚠️⚠️ 그리고 `E2E` 로 세면 **1582건인데 모집중 65%** 로 **오염**이다("End-to-End로 설계" 같은 **업무 범위** 표현이 걸린다 — ⭐ **네 번째 오염 형태: 같은 약자의 다른 뜻**). 쓸 수 있는 값은 `통합 테스트` **176** · `Playwright\|Cypress\|Selenium` **273** 뿐. | **`testing` 의 보강 사이클로 처리한다**(사다리 4순위). 1차 자료는 Fowler 의 `TestPyramid` 로 확실하고, ⭐ **셈 없이 쓸 수 있는 드문 경우**(그 문서가 이미 축을 세워 뒀고 피라미드는 그 안의 배치 문제다). |
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
