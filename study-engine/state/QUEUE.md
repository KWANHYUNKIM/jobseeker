# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| React | `react` | ⭐ **`nodejs` 가 끊긴 링크를 안 남겨 층을 바꾼 자리**(백엔드 → 프론트엔드). **프론트엔드 층은 `design-system` 하나뿐**이다. ⭐⭐⭐ **셈(2026-09-06): `React|리액트` 3713건(모집중 1193) — 이 백과사전이 만난 낱말 중 가장 크다**(`rest` 3372, `CI/CD` 2795 를 넘는다). `Next.js` **1400** · `상태 관리|Redux|Zustand|Recoil` **990** · `렌더링 최적화|리렌더|memo` **397** · `훅|Hooks|useState` 187 · `서버 컴포넌트|RSC` 71 · ⚠️⚠️ **`useEffect` 4건(모집중 0)**. ⭐⭐⭐ **3713 대 4 — 열한째 형태이고 역대 최대 격차다.** ⚠️ **`aliases` 확인 완료**: `React`·`리액트`·`훅`·`Hooks`·`상태 관리`·`Next.js`·`컴포넌트`·`렌더링` 어느 것도 없다. ⚠️⚠️ **그대로 쓰면 3713건짜리 요약본이 된다** — 지금까지 중 가장 위험하다. ⭐⭐⭐ **축을 좁힌다: "가장 많이 쓰이는 훅을 공식 문서가 '탈출구'라고 부른다."** ⭐⭐ **1차 자료가 그 축을 직접 준다** — react.dev 의 **`You Might Not Need an Effect`** 는 **제품이 자기 API 를 쓰지 말라고 설득하는 문서**이고, 그 문서가 속한 섹션 이름이 **`Escape Hatches`(탈출구)** 다. ⭐ **`kotlin`("impractical")·`redis`("we discourage it")·`typescript`(Non-goals)·`javascript`("would be removed") 계보에 또 한 칸** — 여기는 **"이 기능이 필요하다고 느낀다면 대개 설계가 틀린 것"** 이라고 말하는 형태다. 축 넷: ①⭐⭐**"탈출구"라는 분류 자체**(무엇에서 탈출하나 — 선언적 모델) ②⭐⭐⭐**언제 Effect 가 필요 없나**(문서가 목록으로 준다 — 파생 상태·이벤트 처리·부모에게 알리기) ③**언제 정말 필요한가**(외부 시스템과 동기화 — ⭐ 그게 `nodejs`·`redis` 가 말한 '경계'와 같은 자리) ④**셈의 대비**(`상태 관리` 990 vs `useEffect` 4 — **상태는 다들 말하는데 그 상태를 언제 계산하나는 아무도 안 말한다**). **이웃**: `design-system`(⚠️ **축 확인 필수** — 컴포넌트 이야기가 겹칠 수 있다) · `typescript`(동반 54.5%) · `nodejs`(동반) · `javascript`. ⚠️ **`Next.js` 1400건은 별도 낱말로 미룬다**(SSR·서버 컴포넌트는 축이 다르다). | ⭐⭐⭐ **1차 자료: react.dev `You Might Not Need an Effect`** — **부정어가 제목에 있는 드문 문서**다. 그리고 같은 섹션의 `Escape Hatches` 개요. ⭐ **부정어 먼저**: You Might Not Need · shouldn't · unnecessary · don't · avoid · escape hatch · step outside. ⚠️ **react.dev 는 페이지가 크다** — `javascript` 사이클처럼 목차만 올 수 있으니, **못 열면 그 사실을 밝히고 2차 경로를 쓴다**(그 사이클이 세운 규칙) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
