# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| AI 코딩 도구 | `ai-coding` | ⭐⭐⭐ **층을 완전히 바꾼다** — 프로세스를 세 사이클 연속 했다(`agile`→`scrum`, 그 앞은 성능 둘). ⭐⭐⭐ **그리고 132개 문서 중 이 층이 통째로 비어 있다** — `rag`·`embedding`·`vector-db` 는 **AI 를 만드는 쪽**이고, **AI 로 개발하는 쪽**은 하나도 없다. ⭐⭐ **이미 확인된 셈**: `refactoring` 사이클에서 **`AI 도구와 함께 리팩터링` 145건(모집중 47)**. `documentation` 사이클에서도 같은 형태를 여러 공고에서 봤다(베이글코드·엘리스·케이티밀리의서재·아타드·원정대·코나체인). ⚠️ **본 셈은 미확인 — 반드시 센다**: `Copilot\|Cursor\|Claude Code\|Codex\|LLM 도구` · `AI 활용\|AI 도구` · `프롬프트` · `AI 코드 리뷰` · `바이브 코딩` · `AI 페어` · ⭐ **그리고 부정 쪽**(`검증\|리뷰 없이는\|환각\|hallucination\|신뢰할 수 없`). ⭐⭐⭐ **축 후보: "제품이 스스로 적은 한계."** 이 백과사전이 열 번 넘게 해 온 일이다(`redis` "we discourage it", `github-actions` "almost never", `aws` **부정어 없음**). ⭐⭐ **여기서 물을 것**: **AI 코딩 도구의 공식 문서가 "이건 못 한다"를 적는가, 아니면 `aws` 처럼 긍정형 배정만 하는가.** ⚠️ **후자면 그것이 곧 축이다** — 그리고 **`refactoring` 이 남긴 물음과 이어진다**(원문의 세 조건은 누가 하느냐와 무관하다: **작게 · 몇 분 이상 안 깨지게 · 동작이 정의돼 있을 것**). 축 넷: ①⭐⭐⭐**공식 문서가 긋는 선**(부정어를 찾는다) ②⭐⭐**공고가 요구하는 것**(145건이 무엇을 시키나 — 도구 이름? 검증 능력?) ③⚠️**검증 쪽 셈**(생성은 부르는데 검증은 부르나 — ⭐ `documentation`·`performance`·`profiling`·`scrum` 에서 다섯 사이클 연속 본 형태) ④**`refactoring` 의 세 조건을 그대로 적용**. ⚠️ **겹침 확인 필수**: `refactoring`(AI 절이 이미 있다 — **그 절을 넘어서야 한다**) · `code-review` · `testing` · `rag`·`embedding`(만드는 쪽). ⚠️ **aliases 확인 필요**. ⭐⭐⭐ **1차 자료를 여는 그 호출에 "만든 사람이 나중에 무엇을 썼나" 검색을 함께 넣는다** — ⭐ `agile` 에서 이 규칙이 통했고 `scrum` 에서는 **후보를 찾고도 안 열어** 한 걸음 덜 갔다. **이번엔 연다.** | ⭐⭐ **1차 자료 후보**: 도구 제공자의 **공식 문서 중 한계·주의를 적은 페이지**(제품 소개가 아니라 **limitations/best practices** 쪽). ⚠️ **제품 문서는 마케팅에 가까울 수 있다** — ⭐ **부정어가 하나도 없으면 그 사실 자체를 축으로 쓴다**(`aws` 사이클의 방법). ⭐ **부정어 먼저**: cannot · does not · limitations · verify · review · never rely · may be incorrect. ⚠️⚠️ **1차 자료를 못 찾으면 미루고 축을 바꾼다** — 대안: `온보딩` **749건**(⭐ `documentation` 이 "튜토리얼 칸으로 갈라 세지 못했다"고 남긴 자리) 또는 `코드 품질` **651건** |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ 테스트 피라미드 (`testing` 보강) | **`testing` 이 "가장 큰 빈자리"라고 적었으나 셈이 없다** — `테스트 피라미드` **2건**. ⚠️⚠️ 그리고 `E2E` 로 세면 **1582건인데 모집중 65%** 로 **오염**이다("End-to-End로 설계" 같은 **업무 범위** 표현이 걸린다 — ⭐ **네 번째 오염 형태: 같은 약자의 다른 뜻**). 쓸 수 있는 값은 `통합 테스트` **176** · `Playwright\|Cypress\|Selenium` **273** 뿐. | **`testing` 의 보강 사이클로 처리한다**(사다리 4순위). 1차 자료는 Fowler 의 `TestPyramid` 로 확실하고, ⭐ **셈 없이 쓸 수 있는 드문 경우**(그 문서가 이미 축을 세워 뒀고 피라미드는 그 안의 배치 문제다). |
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
