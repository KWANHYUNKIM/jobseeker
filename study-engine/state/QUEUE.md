# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| 개인정보·금융 법령 대응 | `personal-data-law` | **`security-certification` 이 끊긴 링크로 남긴 자리** — 그 문서가 **제도(인증)와 법령은 다른 것인데 실무에서는 한 줄에 온다**고 짚고 넘겼다(크림페이 `전자금융업 관련 규제(ISMS-P, **전자금융감독규정** 등) 준수 및 감사 대응`). 그리고 `dlp`(개인정보보호법) · `audit-log`(FDA·HIPAA) · `network-separation`(금융 규제) · `encryption`(공공·금융 요건)이 **전부 법령을 언급만 하고 지나갔다.** ⭐ **정형 고지문을 걸러 내면 301건(모집중 110)** 로 수요가 크다. 근거: 휴런 `국내·외 보안 컴플라이언스 대응 (**개인정보보호법, 정보통신망법, GDPR** 등)`, 넥스트증권 `**전자금융감독규정** 등 금융 규제 환경에 대한 이해`, 한화생명 `금융앱 보안 요구사항(**전자금융감독규정** 관련 앱 보안 항목)`, 에이아이트릭스 `의료 데이터 프라이버시/규제(**개인정보보호법, IRB**, 데이터 이용 계약)`, 알피 `**HIPAA, GDPR** 등 의료 데이터 규제를 준수한 의료기기 소프트웨어 개발`. ⚠️⚠️⚠️ **새로운 종류의 오탐 — 채용 공고 하단의 정형 법적 고지문이다.** `개인정보보호법` 386건 중 **173건이 `채용서류는 개인정보 보호법에 따라 파기됩니다` 류**였다. `ARIA`(부분 문자열)·`토큰화`(일반 용어)에 이은 사례이고 **이번 것은 모든 공고에 붙는 상용구**라 종류가 다르다. **`채용서류|지원서|파기됩니다|처리방침|반환 요청|지원자의 개인정보` 를 제외하고 센다.** 축: ①**법령과 인증은 다르다**(위반의 결과가 다르다 — 인증은 못 받고, 법령은 처벌받는다) ②⭐**법령이 아키텍처를 정하는 자리**(암호화 대상·보관 기간·파기·접근 기록 → `encryption`·`dlp`·`audit-log`) ③**동의와 목적 제한**(수집한 목적 밖으로 못 쓴다 — 데이터 팀이 가장 자주 막히는 자리) ④**국경을 넘는 이전**(GDPR·클라우드 리전 선택이 법령 문제가 된다) ⑤**개정된다**(현재 시제로 단정하지 않기) | ⭐ **1차 자료** — **국가법령정보센터(law.go.kr)가 공개 HTML 로 현행 조문을 준다** — `network-separation` 에서 조문의 존재와 취지까지 확인했던 그 방식. **GDPR 은 eur-lex 또는 gdpr-info.eu 로 전문이 공개**돼 있다. ⚠️ **`security-certification` 사이클에서 `isms.kisa.or.kr` 는 DNS 실패, `iso.org` 는 403 이었다** — 국내 기관 사이트는 접근이 막힐 수 있으니 **법령 원문(law.go.kr) 을 먼저 시도한다.** ⚠️ **조문을 다룰 때 `network-separation` 의 규칙**(확인한 것/못 한 것을 나눠 적기, 현재 시제 단정 피하기, '인용이 필요하면 현행 조문을 직접 열라'를 본문·표·지뢰 세 곳에) |
| 디자인 시스템 | `design-system` | **`monorepo` 가 끊긴 링크로 남긴 자리**이고, 그 문서에서 **모노레포와 가장 자주 붙어 다니는 낱말**이었다(유모스원·케이티밀리의서재·딥아이·엔닷라이트·아치서울이 한 줄에 함께 적는다). ⭐⭐ **수요가 아주 크다: `디자인 시스템|design system` 544건(모집중 200)**, 넓게(공통 컴포넌트·Storybook 포함) 674건(217). `Storybook` 116건, `디자인 토큰` 25건, `웹 접근성|WCAG|a11y` 254건. **(a)수요 + (b)끊긴 링크가 함께 성립**하고, 이 백과사전에 **프론트엔드 문서가 사실상 없다**는 공백도 메운다. 근거: 컬리 `사내 디자인 시스템 및 **UI 컴포넌트 라이브러리** 설계 및 운영`, 아치서울 `디자인 시스템 컴포넌트·**디자인 토큰** 설계 및 확장, 여러 앱에 걸친 공용 컴포넌트 정리`, ⭐ 엘리스 `디자인 시스템의 **방향성과 운영 원칙**을 정의하고, **디자인 팀과의 싱크 체계 및 협업 창구**를 설계하여 제품 전반의 UI 일관성과 개발 효율을 높입니다`(**조직 문제라는 것을 공고가 직접 적는다**), 헤렌 `공통 UI 컴포넌트 및 디자인 시스템 설계·운영 (XML / Jetpack Compose)`(**웹만의 것이 아니다**). ⚠️ **`디자인`·`컴포넌트` 는 넓다** — `공통 컴포넌트` 만으로는 백엔드의 공통 모듈이 섞이니 **`디자인 시스템` 을 축으로 세고 나머지는 보조로.** ⚠️ **`토큰`은 앞서 `dlp` 에서 오탐이 심했다**(형태소·LLM 토크나이제이션) — **`디자인 토큰` 으로 붙여서만 센다.** 축: ①⭐**컴포넌트 모음이 아니라 '합의를 코드로 만든 것'이다**(엘리스 공고의 '운영 원칙'과 '싱크 체계') ②**디자인 토큰**(색·간격을 이름으로 — 값이 아니라 이름을 공유한다. → `data-contract` 의 '이름이 계약이다') ③**누가 소유하나**(디자인 팀 vs 프론트 팀 — `data-governance` 의 그 문제) ④**버전과 배포**(→ `monorepo` 의 fixed vs independent, `api-versioning` 의 깨는 변경) ⑤⭐**접근성이 여기서 강제된다**(공통 컴포넌트에 한 번 넣으면 전사가 얻는다 — 254건이 그 수요) | ⭐ **1차 자료** — **W3C WAI-ARIA Authoring Practices(APG)** 가 공개 HTML 표준 문서이고 **컴포넌트별 키보드 상호작용·역할이 표로 정리**돼 있다(`ohms-law`·`rbac` 처럼 표준을 직접 여는 형태). **Material Design 3 문서**(토큰 체계)와 **Storybook 문서**(컴포넌트를 문서로 만드는 방식)도 공개. ⚠️ `monorepo` 와 겹치지 않게 — **패키지를 어떻게 함께 움직이나는 그쪽, 여기는 무엇을 공유물로 만드나** |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
