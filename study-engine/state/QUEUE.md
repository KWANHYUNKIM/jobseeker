# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| Next.js | `nextjs` | ⭐⭐ **`react` 가 "별도 낱말"이라고 명시하며 남긴 자리** — 그 문서가 `Next.js` **1400건**을 의도적으로 비웠다(축이 다르다고 적었다). ⭐ **셈(2026-09-06): `Next.js` 1400건(모집중 448)** · `SSR|SSG|ISR` **239** · `Vercel` **163** · `App Router` **116** · `서버 컴포넌트|RSC` **71** · ⚠️⚠️ **`하이드레이션` 4건**. ⭐⭐⭐ **1400 대 4 — 열두째 형태다.** ⚠️ **`aliases` 확인 완료**: `Next.js`·`SSR`·`App Router`·`서버 컴포넌트`·`use client`·`하이드레이션` 어느 것도 없다. ⚠️ **`seo` 와 겹치는지 셌다** — 그 문서에 `SSR` 이 **18회** 나오지만 `서버 컴포넌트`·`하이드레이션`·`use client`·`App Router` 는 **전부 0회**다. ⭐ **축이 다르다**: 저쪽은 **"봇이 내용을 보나"**, 여기는 **"코드가 어디서 도나"**. ⚠️⚠️ **1400건짜리 낱말이라 그대로 쓰면 요약본이 된다.** ⭐⭐⭐ **축을 좁힌다: "코드가 어디서 도는지 이제 우리가 정하는데, 그 경계가 눈에 안 보인다."** **`'use client'` 한 줄이 경계**이고, 그 경계를 잘못 그으면 **서버에만 있어야 할 것이 브라우저로 넘어간다**(⚠️ 비밀·큰 의존성). 그리고 **하이드레이션**이 그 경계에서 일어나는 일인데 **이름이 4건**이다. 축 넷: ①⭐⭐**서버 컴포넌트가 못 하는 것**(1차 자료가 목록으로 준다 — 상태·Effect·브라우저 API) ②⭐⭐⭐**경계가 전염된다**(`'use client'` 를 붙이면 그 아래가 다 클라이언트가 된다 — **한 줄이 트리를 바꾼다**) ③**하이드레이션 불일치**(서버가 그린 것과 브라우저가 그린 것이 다르면 — ⚠️ 4건짜리 이름) ④**캐싱과 무효화**(⭐ `caching` 이 이미 있으니 **겹치는지 먼저 센다**). **이웃**: `react`(정면 — 저쪽이 비워 둔 자리) · `seo`(축 다름, 확인 완료) · `caching`(⚠️ **축 확인 필수**) · `typescript`(동반 69.1%). ⭐ `market` 은 **React 90.2%** — **거의 전부가 React 와 함께**(역방향은 29.4%, 비대칭). | ⭐⭐⭐ **1차 자료: Next.js 공식 문서의 Server/Client Components**(또는 react.dev 의 Server Components) — ⭐ **"서버 컴포넌트에서는 이것을 못 한다"를 목록으로 주는 자리**를 찾는다. ⭐ **부정어 먼저**: can't · cannot · not supported · only works · will not · unsupported · must be · boundary. ⚠️ **Next.js 문서는 페이지가 많다** — 못 열면 그 사실을 밝히고 2차 경로를 쓴다(`javascript` 사이클 규칙) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
