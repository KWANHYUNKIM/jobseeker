# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| AWS | `aws` | ⭐ **`migration` 이 끊긴 링크를 안 남겨 층을 바꾼 자리**(협업/운영을 다섯 연속 했다 → 인프라/클라우드). ⭐⭐⭐ **셈(2026-09-06): `AWS|아마존 웹` 4787건(모집중 1927, 40%) — 이 백과사전 최대 낱말**이다(`React` 3713 을 넘는다). 태그 기준으로도 **1966건 · 27.3%** 로 1위. ⭐⭐⭐ **그런데 `공유 책임|shared responsibility` 는 0건이다.** `rest` 의 `HATEOAS` 0 이래 **두 번째 완전한 0**이고, **4787 대 0** 은 역대 최대 격차다. 그 밖: `EC2|S3|Lambda|RDS|ECS|EKS` **2727** · `비용 최적화|FinOps` **401** · `IAM|최소 권한` **320** · `멀티 클라우드` 248 · `보안 그룹|VPC` 208. ⚠️⚠️ **셈 주의**: `EC2|S3|Lambda...` 가 **모집중 63%** 로 높다 — ⭐ **`Lambda` 가 람다 함수·람다식으로도 쓰인다**(다섯 번째 오염 형태 후보: **같은 이름의 다른 기술**). **반드시 갈라 센다.** ⚠️ **`aliases` 확인 완료**: `AWS`·`클라우드`·`IAM`·`EC2`·`S3`·`공유 책임` 어느 것도 없다. ⚠️⚠️ **4787건짜리 낱말이라 그대로 쓰면 서비스 카탈로그가 된다.** ⭐⭐⭐ **축을 좁힌다: "어디까지가 그들 책임이고 어디부터가 우리 책임인지를 AWS 가 그림으로 그려 놨다 — 그런데 아무도 그 이름을 안 부른다."** ⭐⭐ **1차 자료가 그 축을 직접 준다** — AWS 의 **`Shared Responsibility Model`** 문서. **"Security and Compliance is a shared responsibility between AWS and the customer"** 그리고 ⭐⭐⭐ **"security OF the cloud" vs "security IN the cloud"** 라는 구분을 **그림과 함께** 적는다. ⭐ **이 백과사전이 모은 "제품이 스스로 그은 선" 계보의 가장 명시적인 형태**다 — 앞의 일곱(문장·목록·문서·못 고침·Q&A·복구 절차·공개 기준)과 달리 **선을 그림으로 그려 공개한다.** 축 넷: ①⭐⭐**선이 어디 있나**(OF vs IN — 서비스 종류마다 다르다) ②⭐⭐⭐**셈의 대비**(4787 대 0 — **책임 경계의 이름을 아무도 안 부른다**) ③**비용도 같은 구조인가**(`비용 최적화` 401건 — ⚠️ 1차 자료로 확인할 것) ④**IAM·최소 권한**(320건 — ⭐ `github-actions` 가 "CI 가 AWS 자격증명을 들고 있다"(동반 70.1%)고 적은 그 자리와 이어진다). **이웃**: `github-actions`(⚠️ **축 확인 필수** — 그쪽은 CI 보안) · `authz`(⚠️ 최소 권한이 겹치는지 센다) · `kubernetes`·`docker`·`iac`(동반) · `secrets`. | ⭐⭐⭐ **1차 자료: AWS 의 `Shared Responsibility Model`**(aws.amazon.com/compliance/shared-responsibility-model/) — ⭐ **부정어 먼저**: not responsible · customer is responsible · AWS does not · varies · differs. ⚠️ **aws.amazon.com 은 자바스크립트가 많다** — 못 열면 밝히고 2차 경로를 쓴다(`javascript`·`technical-debt` 사이클 규칙). ⭐ **그리고 못 열면 축을 바꾼다**(`technical-debt` 에서 세운 규칙) — 대안 축: `비용 최적화` 401건 |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ 테스트 피라미드 (`testing` 보강) | **`testing` 이 "가장 큰 빈자리"라고 적었으나 셈이 없다** — `테스트 피라미드` **2건**. ⚠️⚠️ 그리고 `E2E` 로 세면 **1582건인데 모집중 65%** 로 **오염**이다("End-to-End로 설계" 같은 **업무 범위** 표현이 걸린다 — ⭐ **네 번째 오염 형태: 같은 약자의 다른 뜻**). 쓸 수 있는 값은 `통합 테스트` **176** · `Playwright\|Cypress\|Selenium` **273** 뿐. | **`testing` 의 보강 사이클로 처리한다**(사다리 4순위). 1차 자료는 Fowler 의 `TestPyramid` 로 확실하고, ⭐ **셈 없이 쓸 수 있는 드문 경우**(그 문서가 이미 축을 세워 뒀고 피라미드는 그 안의 배치 문제다). |
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
