# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| 기술 부채 | `technical-debt` | ⭐⭐ **`code-review` 가 "별도 낱말로 남긴다"고 명시한 자리** — 그 문서의 기준인 **`code health`** 와 정면으로 이어진다. ⭐ **셈(2026-09-06)**: ⚠️ `기술 부채|technical debt` **227건(모집중 110)** vs `레거시|legacy` **766**(280) vs `리팩터링|리팩토링` **753**(256) vs `마이그레이션` **760**(332). ⭐⭐ **"부채"라는 이름은 덜 쓰고 "레거시"·"리팩터링"을 쓴다** — 열다섯째 형태인데 **이번엔 개념이 아니라 은유가 안 불린다.** ⚠️ **`고도화|점진적 개선` 3425건은 쓰지 말 것** — 너무 넓다(모든 공고가 '고도화'를 쓴다). ⚠️ **`기술 부채` 모집중 비율 48%** 는 정상 범위(30~40%)보다 조금 높다 — **표본이 작아서일 수 있으니 해석하지 않는다**(`git` 사이클 규칙). ⚠️ **`aliases` 확인 완료**: `기술 부채`·`리팩터링`·`레거시`·`technical debt` 어느 것도 없다. ⭐⭐⭐ **축을 좁힌다: "만든 사람이 자기 비유의 오용을 반박한다."** **Ward Cunningham 이 `기술 부채` 라는 말을 만들었고**, 나중에 **그것이 어떻게 오해됐는지 직접 바로잡았다** — ⭐ **"나중에 잘 하겠다는 생각으로 코드를 대충 쓰는 것"이 부채의 주된 원천이라는 이해는 자기 뜻이 아니었다**는 취지다. ⭐⭐⭐ **`rest` 의 Fielding("That is RPC ... Period.")과 정확히 같은 형태**이고, 이 백과사전이 모은 계보 중 **드문 것**이다: 제품이 아니라 **개념을 만든 사람이 오용을 반박**한다. 축 넷: ①⭐⭐**Cunningham 의 원래 뜻**(**모르는 채로 짠 코드**와 **알고도 미룬 코드**는 다르다) ②⭐⭐⭐**오용된 뜻**("대충 짜고 나중에 갚는다") ③**Fowler 의 사분면**(신중/무모 × 의도적/우발적 — ⚠️ 1차 자료로 확인할 것) ④**셈의 대비**(`부채` 227 vs `레거시` 766 — ⭐ **회사는 부채라고 안 부르고 레거시라고 부른다**. 갚을 수 있는 것과 물려받은 것은 다른 말이다). **이웃**: `code-review`(정면 — `code health`) · `api-versioning`·`javascript`(이미 나간 것) · `monorepo` · `testing`?(있는지 확인 — `테스트 코드` 533건). | ⭐⭐⭐ **1차 자료: Ward Cunningham 의 부채 비유 해명**(c2.com 또는 그의 영상 전사) + **Martin Fowler 의 `TechnicalDebtQuadrant`**(martinfowler.com). ⭐ **부정어 먼저**: not what I meant · confused · misunderstood · is not · never meant. ⚠️ **영상 전사는 접근이 어려울 수 있다** — 못 열면 밝히고 2차 경로를 쓴다(`javascript` 사이클 규칙). ⚠️ **그리고 `rest` 문서를 먼저 열어 "만든 사람의 반박"을 어떻게 다뤘는지 본다** — 같은 형태이므로 서술이 겹치지 않게 |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
