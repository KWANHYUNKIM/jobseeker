# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| Node.js | `nodejs` | ⭐⭐ **`javascript` 가 스스로 "가장 큰 빈자리"라고 적은 자리** — `비동기|async|Promise` **1124건**을 그 문서가 안 다뤘고, **자바스크립트의 마이크로태스크·이벤트 루프는 어느 문서에도 없다**(`asyncio` 는 파이썬 것). ⭐ **셈(2026-09-06): `Node.js` 1642건(모집중 592)** · `동시성|병렬` 315 · `Promise|프로미스` 40 · `async/await` 31 · `논블로킹|싱글 스레드` 23 · ⚠️⚠️ **`마이크로태스크|태스크 큐` 2건**. ⚠️ **`aliases` 확인 완료**: `Node.js`·`Promise`·`마이크로태스크`·`논블로킹`·`동시성` 어느 것도 없다. ⚠️ **다만 `이벤트 루프`·`async/await`·`코루틴` 은 `asyncio` 것이니 가져오지 않는다**(`kotlin` 이 `코루틴` 을 내준 것과 같은 규칙). ⚠️⚠️ **`Node.js` 를 그대로 쓰면 런타임 소개가 된다.** ⭐⭐⭐ **축을 좁힌다: "이벤트 루프는 언어가 아니라 호스트가 정한다."** ⭐⭐ **`javascript` 의 `normative optional`("웹 브라우저는 반드시, 아닌 곳은 안 해도 된다")과 정면으로 이어진다** — 그 문서가 "브라우저에서 되던 게 서버 런타임에서 안 될 수 있다"고 했는데, **여기가 그 서버 런타임**이다. 축 넷: ①⭐⭐⭐**이벤트 루프가 ECMAScript 명세에 없다**(명세는 job queue 만 정의하고 **실제 루프는 호스트가 정의**한다 — 그래서 브라우저와 Node 가 다르다) ②⭐⭐**Node 가 자기 루프 단계(phases)를 문서에 적는다**(timers → pending → poll → check → close) ③⭐⭐⭐**그런데 순서를 보장하지 않는 자리가 있다**(`setTimeout` vs `setImmediate` — Node 문서가 **"non-deterministic"** 이라고 직접 적는다) ④**한 스레드가 막히면 전부 막힌다**(`논블로킹` 23건 — ⚠️ 그런데 이름을 대는 공고가 거의 없다). **이웃**: `javascript`(정면 — 저쪽의 '호스트마다 다르다'가 여기 본문) · `asyncio`(⚠️ **축 확인 필수** — 그쪽은 파이썬이고 `JavaScript` 0회였지만 **'이벤트 루프 일반론'이 겹칠 수 있다.** 겹치면 `nodejs` 는 **호스트 차이와 phases 만** 쓴다) · `typescript`(동반 55.3%) · `observability`. | ⭐⭐⭐ **1차 자료: Node.js 공식 `The Node.js Event Loop`** — **런타임이 자기 실행 순서를 단계로 적고, 그중 보장 안 되는 자리를 직접 밝히는 문서**다. ⭐ 부정어 먼저: **non-deterministic** · not guaranteed · depends on · varies · should not rely. 보조로 **`Don't Block the Event Loop`** 문서(있으면). ⚠️ **`javascript` 사이클에서 명세 본문을 못 연 일이 있었다** — Node 문서는 페이지가 작으니 괜찮을 것이나, **못 열면 그 사실을 밝히고 2차 경로를 쓴다**(그 사이클이 세운 규칙) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
