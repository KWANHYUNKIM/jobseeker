# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| Go | `go` | ⭐ **`nextjs` 가 끊긴 링크를 안 남겨 층을 바꾼 자리**(프론트엔드 → 언어). **프론트엔드를 두 번 연속 썼다.** ⭐ **셈(2026-09-06): `market` 기준 848건(11.8%, 층 "언어")** — ⚠️⚠️ **직접 셈 `\bGo(lang)?\b` 은 1414건이지만 오염이 극심하다**("Go to"·회사명 등, 모집중 비율이 65%로 비정상). **market 숫자를 쓰고, 본문 셈은 반드시 문장을 읽어 가른다.** ⭐⭐⭐ **결정적 대비: `고루틴|goroutine` 1건(모집중 0).** 이 언어의 가장 상징적인 기능인데 **한 건**이다 — `rest` 3372 vs `HATEOAS` 0 계보의 **가장 극단**. 그 밖: `gRPC` **243** · `에러 처리` 148 · `제네릭` **12**. ⚠️⚠️ **`채널|channel` 889건은 쓰지 말 것** — 채널톡·유통 채널·마케팅 채널이 대부분이다(`load-balancer` 의 `Nginx`, `android` 의 `생명주기` 와 같은 형태). ⚠️ **`aliases` 확인 완료**: `Go`·`Golang`·`고루틴`·`제네릭`·`에러 처리` 어느 것도 없다. ⭐⭐⭐ **축을 좁힌다: "뺀 것으로 만든 언어."** ⭐⭐ **1차 자료가 그 축을 통째로 준다** — **Go FAQ 는 `Why does Go not have X?` 형태의 질문이 줄줄이 있는 문서**다(예외·제네릭·상속·어서션). ⭐ **`typescript` 의 Non-goals, `react` 의 `You Might Not Need an Effect` 에 이은 계보의 다음 칸이고 형태가 또 다르다** — 저쪽은 목록과 설득이었는데 **여기는 Q&A 로 "왜 안 넣었나"를 하나씩 답한다.** 축 넷: ①⭐⭐**FAQ 가 답하는 "왜 없나"들**(무엇을 얻으려고 뺐나 — 컴파일 속도·읽기 쉬움) ②⭐⭐⭐**에러를 값으로 다룬다**(예외가 없다 — `에러 처리` 148건과 이어진다) ③**동시성**(⚠️ **`asyncio` 와 축이 겹치는지 먼저 센다** — 그쪽은 한 스레드/이벤트 루프, 여기는 고루틴/채널로 모델이 다르다) ④**어디에 쓰이나**(`market` **Kubernetes 40.0%** · `gRPC` 243건 — **인프라 도구의 언어**). **이웃**: `kubernetes`(동반 40%) · `asyncio`(⚠️ 축 확인 필수) · `service-mesh`/`api-gateway`(Go 로 쓰인 것들) · `typescript`(계보). | ⭐⭐⭐ **1차 자료: Go 공식 FAQ**(`go.dev/doc/faq`) — **"Why does Go not have..." 절들이 곧 이 문서의 뼈대**다. 보조로 `Effective Go` 의 에러 관련 절. ⭐ **부정어 먼저**: does not have · we did not · omitted · deliberately · left out · not worth · complexity. ⚠️ **페이지가 크면 목차만 올 수 있다** — 못 열면 밝히고 2차 경로를 쓴다(`javascript` 사이클 규칙) |
| 상태 관리 | `state-management` | ⭐⭐ **`react` 가 스스로 "가장 큰 빈자리"라고 적은 자리** — `상태 관리|Redux|Zustand|Recoil` **990건(모집중 313)** 인데 **Redux·Zustand·React Query 의 공식 문서를 하나도 안 열었다**고 그 문서가 밝혔다. ⚠️⚠️ **alias 충돌 주의**: `상태 관리` 를 **`react` 가 갖고 있다.** ⭐ **그런데 `react` 는 그것을 1차 자료로 다루지 않았으므로**(`kotlin` 이 `코루틴` 을 `asyncio` 에 내준 규칙의 역방향) **새 문서가 가져오고 `react` 에서 뺀다** — 그때 `react` 의 `index.json` `aliases` 도 함께 고친다. ⭐ **축 후보: "어디에 두나" 가 아니라 "언제 서버 것이 클라이언트 것이 되나"** — 서버에서 온 데이터(캐시)와 화면의 상태(폼·모달)는 다른 물건인데 한 낱말로 불린다. `react` 의 `useEffect` 4건, `nextjs` 의 서버 컴포넌트와 정면으로 이어진다. ⚠️ **축이 안 서면 미룬다.** | 1차 자료 후보: **TanStack Query 문서의 "서버 상태 vs 클라이언트 상태" 설명**, Redux 의 "When should I use Redux?" 절(⭐ **부정어가 있는 자리**), Zustand README. ⚠️ **`caching` 과 겹치는지 먼저 센다** |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
