# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| Kotlin | `kotlin` | ⭐ **후보 조사로 골랐다 — 그리고 층을 바꿨다**(인프라·API 가 여섯 사이클로 두꺼워졌다). ⚠️ **`aliases` 로 확인 완료**: `Kotlin`·`코틀린` 어느 문서에도 없다(이번 사이클의 실수 재발 방지). ⭐⭐ **셈: 1072건(모집중 448)** — 후보 목록에 적힌 464(tech_relations 태그 기준)의 **두 배 이상**이다. 이웃: `Java` 4612 · `Spring` 2787 · `Android` 1345. ⭐⭐⭐ **그런데 대표 특징이 안 불린다: `코루틴` 108건 · `널 안전|null safe|NPE` **7건(모집중 0)**.** `rest`(3372 vs HATEOAS 0) · `graphql`(370 vs DataLoader 2) · `service-mesh`(163 vs 사이드카 2) · `db-index`(939 vs 슬로우 쿼리 19)에 이은 **일곱째**다. ⭐⭐⭐ **그리고 `postgres-mysql` 과 똑같은 형태가 하나 더 있다 — 공고가 둘을 묶어 쓴다**: 빗썸 `**Java / Kotlin** 기반 Spring Boot 서비스 개발 경험`, 인터엑스 `**Kotlin (또는 Java)** 및 SpringBoot 기반의 백엔드 개발`, 큐엠아이티 `**JVM 생태계(Java, Kotlin, Spring, JPA)**에 대한 경험`, 리브리티 `Kotlin, Java, Spring`. **시장은 둘을 가르지 않는다.** ⭐ 축 초안: **"Java 가 있는데 왜 Kotlin 인가 — 그런데 공고는 둘을 안 가른다"** — 그리고 **가장 큰 차이(널 안전)가 7건**이라는 간극이 그 축의 증거다. ⚠️⚠️ **경계 — 이웃이 넷이다**: `java` · `java-in-finance`(금융권은 왜 Java 인가) · `spring` · `gc`(JVM 메모리). **이 문서는 그 넷의 옆에 서는 "선택" 문서**여야 한다 — 언어 문법 소개가 아니라 **"같은 JVM 위에서 왜 갈리나"**. ⭐ `postgres-mysql` 에서 세운 대로 **기능 비교표를 옮기면 실패**이고, `when_to_use` 의 `over` 가 문서의 중심이다. 축: ①⭐**공고가 둘을 묶는다**(셈) ②**널 안전 — 7건인데도 이 언어의 존재 이유**(컴파일 시점에 막는 것과 런타임에 터지는 것) ③**코루틴 108건**(→ `backpressure`·`saga` 와 이웃일까 확인) ④**Android 의 사정**(1345건 — 구글이 공식 언어로 정한 것이 시장을 바꿨나) ⑤**서버에서는 왜 천천히 오나**(Spring 2787 대비) | ⭐ **1차 자료** — **Kotlin 공식 문서의 "Null safety"·"Comparison to Java"** 와 **"Kotlin/Java interop"** 문서. ⭐⭐ **`rest` 에서 배운 대로 부정어(cannot·not supported·caveat·limitations)를 먼저 검색**하고, 특히 **자바 상호운용의 한계(플랫폼 타입 등)를 공식 문서가 어떻게 적는지**가 축이 될 수 있다 — **"널 안전을 자랑하는 언어가 자바와 만나는 자리에서 무엇을 포기하는가."** ⚠️ 안드로이드 쪽 자료는 별도이니 범위를 먼저 정한다 |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
