# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|
| Spring | `spring` | **(b) 끊긴 링크 — `java` 가 "동반 44.4%, 공고의 절반이 함께 요구한다"며 넘겼다.** **(a) 수요가 크다** — `tech_relations` 기준 Spring 자체가 백엔드 레이어 상위이고, `Spring Boot` 가 따로 또 잡힌다(Java 공고의 24.5%). 축은 **왜 프레임워크가 필요했나** — DI 가 무엇을 풀었나(객체 생성과 사용의 분리, 테스트 대체), AOP·트랜잭션 경계(`@Transactional` 이 실제로 하는 일과 프록시 함정: 자기 호출·private), Boot 의 자동 설정이 무엇을 감췄나, JPA/Hibernate 의 지연 로딩·N+1. **공고가 프레임워크 버전까지 우대로 적는다**(`Spring Boot 3.x 우대` — 에이치투오호스피탈리티) | Spring 공식 문서(docs.spring.io)와 Spring Boot 레퍼런스, Jakarta Persistence 사양. **URL 은 `WebSearch` 로 확인하고 건다.** ⚠️ `java` 와 겹치지 않게 JVM·GC·가상 스레드는 링크로 넘긴다. 금융 SI 맥락은 `java-in-finance` 에 있다 |
| MQTT | `mqtt` | **(b) 끊긴 링크 — `modbus` 가 게이트웨이의 반대편으로 가리킨다.** **(c) 공고에서 Modbus 와 한 문장에 나온다** — `Modbus, MQTT, TCP/IP 소켓 통신 등 디바이스-서버 간 통신 프로토콜을 직접 설계하거나 구현해본 경험`(헤리트), `Serial, TCP/IP, UDP, MQTT, ASN.1`(바이다). 축은 **폴링의 반대** — 발행/구독으로 뒤집힌 방향, QoS 0/1/2 와 **중복 전달이 왜 생기나**(→ `idempotency` 와 바로 이어진다), 유지 메시지·유언(LWT)·킵얼라이브, 토픽 설계. **`modbus` 가 '폴링 주기가 곧 지연'이라고 쓴 자리의 답이 이 문서다** | MQTT 사양은 **OASIS 표준**(mqtt.org 에서 안내)이고 v3.1.1·v5.0 이 갈린다 — **버전을 밝혀 써야 한다.** URL 은 `WebSearch` 로 확인하고 건다 |
| 멱등성 | `idempotency` | **(c) 회의에서 그냥 쓰이는 말.** 결제·주문 공고의 자격요건에 `멱등성`이 설명 없이 등장한다. 재시도·중복 요청·네트워크 타임아웃과 한 몸이라 백엔드 면접의 단골이기도 하다. 정의는 한 줄인데 *언제 멱등키를 쓰고 언제 분산 락을 쓰나*를 가른 글은 드물다 | HTTP 메서드 정의(RFC 9110), Stripe·Toss 결제 API 의 idempotency key 문서, 국내 커머스 기술블로그의 재시도 글 |

## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| BigDecimal | `java-in-finance` 가 가리키지만 **낱말이 작다.** `java` 문서의 한 절이나 `ohms-law` 처럼 독립 문서 둘 다 가능하다. 지금 쓰면 "반올림 모드를 명시하라" 두 문단으로 끝난다 | `java` 를 쓴 뒤, 그 문서에 흡수됐는지 아니면 여전히 끊긴 링크로 남는지 보고 정한다 |
| Kubernetes | 공고 1,183건(15.8%)으로 수요는 크지만, **이 엔진의 값은 '설명이 없는 말'에서 나온다.** 쿠버네티스는 한국어 자료가 이미 두껍다. 쓰더라도 `when_to_use`(언제 안 쓰나)만 남기는 형태가 맞다 | 큐가 비고 수요 순으로 훑을 때 |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
