# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| JavaScript | `javascript` | ⭐⭐ **`typescript` 가 남긴 끊긴 링크 — 네 사이클 만에 사슬이 다시 이어졌다.** 그 문서가 스스로 **"가장 큰 빈자리"**라고 적었다: **TypeScript 의 결과물은 언제나 자바스크립트인데 그 언어가 이 백과사전에 없다.** ⭐ **셈(2026-09-06): 2397건(모집중 743)** · `비동기|async|Promise` **1124** · ⚠️ `프로토타입|클로저` 328(**오염 큼** — '프로토타입 제작·시제품'이 섞인다, 문장을 읽어 가른다) · `ES6|ES20xx|최신 문법` 258 · `브라우저 호환|폴리필|Babel` 73 · ⚠️ `이벤트 루프` 18(**alias 를 `asyncio` 가 이미 갖고 있다**). ⚠️ **`aliases` 확인 완료**: `JavaScript`·`자바스크립트`·`ES6`·`프로토타입`·`클로저`·`비동기` 어느 것도 없다. ⚠️ **다만 `이벤트 루프` 는 `asyncio` 것이니 가져오지 않는다**(`kotlin` 이 `코루틴` 을 `asyncio` 에 내준 것과 같은 규칙). ⚠️⚠️ **그대로 쓰면 언어 문법 요약본이 된다** — 2397건짜리 낱말이라 더 위험하다. ⭐⭐⭐ **축을 좁힌다: "고칠 수 없는 언어."** **웹은 되돌릴 수 없다** — 한번 나간 문법은 **영원히 지원해야 한다**(`android`·`ios` 의 "옛 버전이 안 사라진다"가 **언어 자체에서 일어난 형태**). ⭐⭐ 그리고 `typescript` 문서의 **"왜 건전하게 안 만들었나"** 가 여기서 답을 얻는다: **기존 코드를 안 깨려고.** 축 넷: ①⭐⭐**명세가 스스로 legacy 라고 부르는 부록**(→ 1차 자료) ②**그래서 남은 것들**(`==`, `typeof null`, 호이스팅 — ⚠️ **나열이 아니라 "왜 못 고쳤나"를 쓴다**) ③**폴리필·트랜스파일**(73건 — 새 문법을 옛 브라우저에서 쓰는 값) ④⭐**비동기 1124건**(⚠️ **`asyncio` 가 이벤트 루프를 이미 다뤘으니 겹치는 부분을 먼저 센다**). **이웃**: `typescript`(정면 — 저쪽의 '왜'가 여기 답이 있다) · `asyncio`(⚠️ 축 확인 필수) · `browser-rendering`?(있는지 확인) · `seo`. | ⭐⭐⭐ **1차 자료: ECMAScript 명세의 `Annex B`** — **명세가 자기 일부를 "legacy"·"normative optional"로 따로 떼어 적어 둔 자리**다. `typescript` 의 **Non-goals** 와 같은 계열이고, ⭐ **"없애고 싶은데 못 없앤다"고 명세가 직접 말하는 드문 형태**. 보조로 **TC39 프로세스 문서**(제안이 어떻게 표준이 되나 — 되돌릴 수 없는 이유). ⭐ **부정어 먼저**: legacy · deprecated · normative optional · not recommended · discouraged · web browsers only · must not be used |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
