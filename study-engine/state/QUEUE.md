# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| 코드 리뷰 | `code-review` | ⭐⭐ **`git` 이 스스로 "가장 큰 빈자리"라고 적은 자리** — `코드 리뷰|Pull Request` **1610건(모집중 561)** 으로 그 문서 안 최대 조각인데 **한 줄도 안 썼다.** ⭐ **셈(2026-09-06)**: `코드 리뷰|PR` **1610** · `기술 부채|리팩터링` **835**(262) · `테스트 코드|단위 테스트` 570 · `린트|정적 분석` 229 · ⚠️ `리뷰 문화|리뷰어` **188** · `페어 프로그래밍` 61. ⭐⭐ **또 그 형태다 — 1610 대 188.** **리뷰를 요구하면서 리뷰하는 쪽(리뷰어)의 이름은 안 부른다**(열넷째). ⚠️⚠️ **alias 이관이 필요하다**: `코드 리뷰` 를 **`git` 이 갖고 있다.** ⭐ 그런데 그 문서는 코드 리뷰를 **1차 자료로 다루지 않았고 스스로 빈자리라고 적었다** — `state-management` 가 `react` 에서 `상태 관리` 를 가져온 것과 같은 절차로 옮긴다. **세 곳을 함께 고친다**: `git` 의 `aliases`, `index.json` 의 그 항목, `git` 의 `open_questions`. ⭐⭐⭐ **축을 좁힌다: "리뷰의 기준은 '완벽한가'가 아니다."** 1차 자료가 그것을 직접 적는다 — **Google 의 `Code Review Developer Guide`** 에 **"The Standard of Code Review"** 라는 절이 있고, ⭐ **"완벽하지 않아도 전체 코드 건강을 개선하면 승인하라"** 부류의 문장과 **"There is no such thing as 'perfect' code"** 가 있다고 알려져 있다. ⭐ `git`("a bad idea")·`go`("Why does Go not have X?")·`react`("You Might Not Need")에 이은 계보이고 형태가 또 다르다: **회사가 자기 기준을 문서로 공개하고, 그 기준이 "완벽"이 아니라고 못 박는다.** 축 넷: ①⭐⭐**기준이 무엇인가**(완벽 vs 개선 — 1차 자료의 핵심) ②⭐⭐⭐**리뷰가 병목이 될 때**(⚠️ **`git` 의 축과 이어진다** — 리뷰는 "공유되기 전에 보는 자리"인데, 그 자리가 막히면 팀이 선다) ③**무엇을 보나**(설계·가독성·테스트 — ⚠️ `테스트 코드` 570건과 이어진다) ④⚠️**리뷰어라는 역할**(188건 — **받는 쪽 이야기는 많은데 하는 쪽 이야기는 적다**). **이웃**: `git`(정면 — alias 이관) · `github-actions`(자동 검사는 그쪽) · `design-system`(리뷰가 계약을 지키는 자리) · `기술 부채`?(835건, 문서 없음 — 다음 후보). | ⭐⭐⭐ **1차 자료: Google `Engineering Practices — Code Review Developer Guide`**(`google.github.io/eng-practices/review/`) — 특히 **`The Standard of Code Review`** 와 **`Speed of Code Reviews`** 두 절. ⭐ **부정어 먼저**: not perfect · no such thing · should not · never · avoid · don't block. ⚠️ **페이지가 여러 개로 나뉘어 있다** — 못 열면 밝히고 2차 경로를 쓴다(`javascript` 사이클 규칙) |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
