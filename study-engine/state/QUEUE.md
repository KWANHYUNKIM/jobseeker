# 대기열 — 쓸 낱말

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(낱말)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 인트로 한가운데를 잘라 먹는다. `"\n## 대기"`(줄 시작)로 찾는다.

큐에 올릴 자격은 셋 중 하나다 (PROMPT.md 2⁗단계).
**(a) 수요가 큰데 문서가 없다** / **(b) 이미 쓴 문서가 계속 가리킨다(끊긴 `related`)** /
**(c) 공고·블로그에는 나오는데 어디에도 설명이 없는 말.**

## 대기

| 낱말 | slug | 왜 이 낱말 | 무엇을 읽을 수 있나 |
|---|---|---|---|



| 애자일 / 스크럼 | `agile` | ⭐⭐⭐ **층을 바꾼다** — 성능을 두 사이클 연속 했다(`performance`→`profiling`). 여기는 **프로세스/일하는 방식**이고 **130개 문서 중 이 층이 통째로 비어 있다.** ⭐⭐ **셈(2026-09-06): `애자일|agile|스크럼|scrum|스프린트` 662건(모집중 348)** · `회고|포스트모템|장애 보고` 255(145). ⚠️ **나머지는 미확인 — 반드시 센다**: `데일리|스탠드업` · `백로그|우선순위` · `칸반` · `짝 프로그래밍|페어` · `이터레이션` · `OKR|목표 관리` · `자율|수평 문화`. ⭐⭐⭐ **축 후보: 선언문의 마지막 줄이 잘린다.** 네 줄이 전부 **"A over B"** 형식인데 그 밑에 **"That is, while there is value in the items on the right, we value the items on the left more."** 가 붙어 있다 — ⚠️ **오른쪽도 가치 있다고 명시**한다. ⭐⭐⭐ **`performance` 의 Knuth 인용 잘림과 정확히 같은 형태다**(앞부분만 남으면 뜻이 뒤집힌다) — 📌 **이 백과사전의 오용 계보 다섯 번째**: 낱말이 잘못 붙음(`refactoring`) → 한 낱말이 네 일을 덮음(`documentation`) → 인용이 잘림(`performance`) → 그림을 잘못 읽음(`profiling`) → **선언의 단서 조항이 잘림.** ⭐⭐ **그리고 셈으로 확인할 수 있다** — 선언문의 네 줄에 나오는 낱말(**개인과 상호작용 / 작동하는 소프트웨어 / 고객과의 협력 / 변화에 대응**)을 각각 세고, **`스크럼`·`스프린트` 같은 절차 이름과 비교**한다. ⚠️ **절차 이름이 크고 가치 이름이 작으면 그게 축이다.** 축 넷: ①⭐⭐⭐**잘린 단서 조항** ②⭐⭐**12원칙 중 공고가 부르는 것은 몇 개인가**(⭐ 원문 확인) ③**회고 255건** — 유일하게 이름이 붙은 실천인가 ④⚠️**`애자일` 과 `스크럼` 을 갈라 센다**(선언과 특정 방법론은 다르다). ⚠️ **겹침 확인**: `code-review` · `documentation`(회고=설명 칸) · `oncall`(포스트모템) · `technical-debt`. ⚠️ **aliases 확인 필요**(`애자일`·`스크럼`·`스프린트`·`회고`). ⭐⭐⭐ **1차 자료를 여는 그 호출에 실패 사례 검색을 함께 넣는다** — 다섯 사이클 연속 지켰다(⚠️ 다섯 다 2차 자료뿐이었고 매번 기록했다. ⭐ **애자일은 비판 문헌이 1차급으로 존재할 가능성이 있다** — 선언문 서명자들이 쓴 글). | ⭐⭐⭐ **1차 자료: agilemanifesto.org** (+ **agilemanifesto.org/principles.html** — 12원칙 전문). ⭐ **아주 짧고 확실하다.** ⭐ **부정어 먼저**: while there is value in the items on the right · over · not. ⚠️ **못 열면 축을 바꾼다** — 대안: `온보딩` **749건**(`documentation` 이 "튜토리얼 칸으로 갈라 세지 못했다"고 남긴 자리) 또는 `코드 품질` **651건** |
## 미룸

| 낱말 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| ⭐ 테스트 피라미드 (`testing` 보강) | **`testing` 이 "가장 큰 빈자리"라고 적었으나 셈이 없다** — `테스트 피라미드` **2건**. ⚠️⚠️ 그리고 `E2E` 로 세면 **1582건인데 모집중 65%** 로 **오염**이다("End-to-End로 설계" 같은 **업무 범위** 표현이 걸린다 — ⭐ **네 번째 오염 형태: 같은 약자의 다른 뜻**). 쓸 수 있는 값은 `통합 테스트` **176** · `Playwright\|Cypress\|Selenium` **273** 뿐. | **`testing` 의 보강 사이클로 처리한다**(사다리 4순위). 1차 자료는 Fowler 의 `TestPyramid` 로 확실하고, ⭐ **셈 없이 쓸 수 있는 드문 경우**(그 문서가 이미 축을 세워 뒀고 피라미드는 그 안의 배치 문제다). |
| ⭐ `go` 의 동시성 절 (보강) | **`go` 문서가 스스로 "가장 큰 구멍"이라고 적었다** — 고루틴·채널·`select`·CSP 를 한 줄도 못 썼다. ⚠️ **셈이 없어서**다(`고루틴` 1건, `채널` 은 채널톡 오염으로 사용 불가). ⭐ `asyncio` 와 겹치지도 않는다(그쪽에 `Go` 0회). | **1차 자료를 찾으면 바로** — `Effective Go` 의 동시성 절, 또는 `Go Blog` 의 "Share Memory By Communicating". ⭐ **셈 없이 쓰는 것이 허용되는 드문 경우**다(언어의 핵심 기능이고 1차 자료가 확실하다). 보강 사이클(사다리 4순위)로 처리한다. |
| 시맨틱 버저닝 (`semantic-versioning`) | **셈에서 3건(모집중 1)** — `시맨틱 버저닝\|semantic version\|semver\|MAJOR.MINOR` 로 이름을 대는 공고가 사실상 없다. `changeset\|Conventional Commits\|semantic-release` 까지 넓혀도 10건(모집중 2). `api-versioning` 이 끊긴 링크로 남겼으나 **근거가 이 백과사전 최저 수준**이라 지금 쓰면 '규격 요약본'이 된다. | **`monorepo` 를 쓴 뒤** — 그 문서가 `fixed vs independent` 버저닝을 다루면서 이 낱말을 다시 가리킬 것이다. 또는 공고에서 `semver` 를 이름으로 대는 곳이 10건을 넘으면. |

## 재시도 안 함

| 낱말 | 왜 | 다시 꺼낼 조건 |
|---|---|---|
| (없음) | | |
