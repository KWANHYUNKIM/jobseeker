# docker-compose.prod.yml 용 환경변수 템플릿.
#   cp .env.example .env  후 실제 값을 채운다. .env 는 절대 커밋하지 않는다.

# Cloudflare Zero Trust → Networks → Tunnels 에서 터널 생성 시 발급되는 토큰.
# "Install and run a connector" 화면의 명령어에 들어있는 긴 문자열이다.
CLOUDFLARE_TUNNEL_TOKEN=
