# 대기열 — 브리핑을 쓸 회사

`validate.py --gaps` 의 사다리 3순위가 이 파일의 "대기" 표 **맨 위 한 줄**을 읽는다.
표의 첫 열(회사명)만 기계가 읽는다 — 나머지 열은 사람과 다음 사이클을 위한 근거다.

> ⚠️ 이 문서를 스크립트로 고칠 때 `s.index("## 대기")` 로 자르지 말 것.
> 위 문장에 같은 글자가 있어 **인트로 한가운데를 잘라 먹는다.** 실제로 두 번 당했다.
> 자르려면 `"\n## 대기"`(줄 시작)로 찾거나 `## 미룸` 기준으로 뒤에서 자른다.

후보를 여기 옮기기 전에 PROMPT.md 2⁗단계로 거른다. `--gaps` 가 뽑아 주는 목록은
**공고 수로 줄 세운 것일 뿐** 지원할 만한 자리인지, 읽을 자료가 있는지는 보지 않는다.
**공고 수보다 (a) 본문 있는 공고 수와 (b) 경력 요건 분포를 먼저 센다**(STATE.md 참조).

## 대기

| 회사 | 모집중 / 본문있음 | 왜 이 회사 | 읽을 수 있는 것 |
|---|---|---|---|
| 왈로우 | 1 / **1건 본문 있음 — `개발` 공고가 아니다** | **공고에 적힌 회사명은 브랜드다 — 법인은 `워로브라더스`**(`※ 벤딕트, 언템드, 왈로우, 퍼플래빗은 워로브라더스의 브랜드 입니다.`). 브리핑은 법인 단위로 쓰고 `aliases` 에 `왈로우` 를 넣어야 뷰어가 공고와 잇는다. ⚠️ **먼저 밝힐 것: 이 한 건은 소프트웨어 자리가 아니다.** `상품제품 기획개발자(소형가전, 차량/캠핑용품)` 이고 공고가 못을 박는다 — `IT 서비스 기획이 아니고 상품/제품 기획/개발 포지션입니다.` 그래도 대기에 올리는 이유는 **회사 쪽 재료가 이 목록에서 드물게 두껍기 때문**이다. 공고 본문에 **2018년 2명·매출 2억 출발 → 연평균 250%·누적 20,000% 성장**, **외부 투자를 받지 않았다**는 문장이 그대로 있고, **브랜드가 넷으로 갈려 있다**(`벤딕트` 차량용품 / `언템드` 캠핑 / `왈로우` / `퍼플래빗`). **`CX 기반 커머스`를 자칭하고 `커뮤니티 App·플랫폼 비즈니스로 확장 중`**이라고 적어, **제조·유통 회사가 왜 개발 조직을 만드는가**를 `signals` 로 읽을 수 있는 첫 사례다. 경력 문은 **`경력 2-10년`**으로 넓고 근무지는 **경기 하남시**(비강남 조건) | **공고 본문이 회사 소개서 겸 언론 링크 모음이다** — `아기유니콘 육성사업(수익성장형)` 선정, `벤딕트 2025 대한민국 브랜드 파워 1위 6년 연속`, `하남시 2024 지속성장 일자리창출 우수기업`, **대표 이준혁 인터뷰**(`사람과 기술, 부족함 속에서 함께 만드는 새로움`)까지 링크가 붙어 있어 `company.business`·`scale` 을 `confirmed` 로 받칠 수 있고 **`people` 에 넣을 서명 있는 공개 발언이 최소 한 건 확보된다.** 공식 사이트 `wolobrothers.com` + 제품 페이지(전기차 충전기 `ANY CHARGER`, 무선 에어건 `써지`, QR 주차번호판 `SAFE LINK`, 차량 방향제 `애트모어`)로 **도메인을 제품군으로 나눌 수 있다.** ⚠️ 비상장·무투자라 사업보고서·투자 공시가 없다 — `salary` 는 **아기유니콘 공시·공고의 성장률 문장·같은 규모 시장 밴드**로 잡고 못 잡으면 `open_questions` 에 남긴다 |
| 포밸류소프트 | 8 / **8건 전부 본문 있음 — 실제 자리는 5~6개** | **2018년 설립, 사원 38명·매출 67.1억의 금융 SI 강소기업**(고용노동부 선정)인데, 공고가 **전통 SI에서 AI 에이전트로 갈아타는 중간 지점을 그대로 드러낸다** — 같은 회사가 한쪽에서 `Java/Spring/Oracle/Tibero/JSP/jQuery` 로 **NH농협 올원뱅크·오픈플랫폼·스마트고지** 차수를 돌리고, 다른 쪽에서 `FastAPI/LangChain/LangGraph/MCP/pgvector/RAG` 를 요구한다. **지금까지 쓴 회사 중 "SI 회사의 AI 전환"을 한 회사 안에서 비교할 수 있는 첫 사례다.** 그리고 금융권이라 **망분리·온프레미스·전문(Flat File) 연동·AES/SEED/ARIA 암호화** 같은 **일반 웹 커리어에서 안 나오는 학습 항목**이 자격요건 원문에 박혀 있다. ⚠️ **중복과 계약직 주의** — 원천징수/환원·종합수익관리 두 자리가 원티드·점핏에 겹쳐 올라와 8건이 실제로는 5~6개다. **경력 문이 대부분 7~17년 고급**이고, **중급 문은 사람인 `Java 개발 정규직(사원~대리) 경력 3~5년` 단 1건**이다. 잡코리아 `[React]` 건은 **6개월 계약직 또는 프리랜서**로 정규직이 아니다 | **공고 본문 자체가 회사 소개서다** — 설립연도·자본금·매출·사원수가 원문에 적혀 있어 `scale` 을 처음으로 공고에서 직접 받칠 수 있다. **고객사가 실명(NH농협)으로 나오고** 참여 사업(올원뱅크 슈퍼플랫폼·오픈플랫폼·스마트고지·마이데이터)이 특정돼 있어 **도메인을 농협 공개 자료로 재구성**할 수 있다. `원천징수/환원`·`종합수익관리` 는 증권·금융 업무 용어라 **업무 자체를 공개 자료로 풀어낼 수 있다.** 마감된 공고의 주소가 `강남구 논현로` 이고 현재가 `관악구 남부순환로` 라 **본사 이전 시점도 공고 이력에 남아 있다.** ⚠️ 비상장이라 사업보고서는 없다 — `salary` 는 공고·잡플래닛류 대신 **강소기업 공시·매출/인원(67.1억 ÷ 38명)** 으로 밴드를 추정해야 한다 |
| 엑셈 | 18 / **실제 자리는 8~10개** | **국산 APM·DB 성능 모니터링**(MaxGauge) 회사이고 **2015년 코스닥 상장사**다 — **사업보고서가 공시돼 있어 지금까지 계속 막혔던 `salary`·`scale` 근거를 처음으로 원문에서 잡을 수 있다.** 그리고 **중급 이하 문이 이 목록에서 가장 넓다**(`경력 1~3년`, `경력2년↑`×4, `경력무관`×3). ⚠️ **중복이 심하다** — 같은 자리가 사람인·잡코리아에 겹쳐 올라오고(`풀스택`·`데이터 엔지니어(ETL)`·`[빅데이터/AI]` 계열이 각각 2~5회), **`계약직` 표기가 여럿**이라 골라 쓸 때 확인이 필요하다 | **사업보고서(전자공시·KRX KIND)** — 인원·평균급여·매출 구성이 들어 있다. 제품이 `MaxGauge`·`DataSaker`(SaaS)·`exemONE`으로 갈려 있어 **온프레미스 → 클라우드 전환 국면**을 공고와 맞춰 읽을 수 있다 |


## 미룸

| 회사 | 왜 미뤘나 | 언제 다시 볼까 |
|---|---|---|
| 메가존 (메가존클라우드와 **다른 법인**) | **공고 1건뿐이라 회사 브리핑 단위가 안 된다.** 다만 그 한 건(`[메가존] Physical AI 포지션 채용 진행`) 안에 **자리가 셋**이고 **`Physical AI LAB` 이라는 새 조직**이다 — `Physical AI 엔지니어`(**졸업 예정자 포함**, UR·Unitree·Franka 상용 로봇, Isaac Sim·MuJoCo, ROS/ROS2, RFM·VLA 파인튜닝) / `Physical AI Business Development`(**China Unit**, 신입~8년, **중국어 필수**, 중국 로봇업체와 파트너십) / `로봇 교육 기획·운영`(커리큘럼·강의). **앞서 쓴 플라잎과 정면 비교가 된다** — 플라잎은 로봇을 직접 만들고 여기는 **상용 로봇을 사서 쓴다**. 회사 소개는 `1998년 웹 에이전시/호스팅` 출발, `광고/영상` 확장 | **Physical AI 공고가 늘면** 곧바로 꺼낸다. 지금은 한 건이라 `salary`·`domains` 를 공고로 못 받친다 |
| 캐치잇플레이 | **제주 제주시 + 재택근무 가능**이라 비수도권 독자에게 드문 조건이고 `9.7억 건의 학습 행동 데이터` 라는 재료도 있다. 다만 **모집중 8건 중 상당수가 같은 자리 중복**(데이터 엔지니어·ML 엔지니어가 표기만 바꿔 반복)이라 **실제 자리가 서넛**으로 보인다 | 자리 수가 늘거나, **제주·재택 조건을 우선으로 볼 때** 가장 먼저 꺼낸다 |
| 넥스트증권 | **19건이 전부 개발이고 본문도 전부 있다**(Backend·Data·AI·DevOps·PM). 다만 **3~8년 서울 금융권**이라 앞서 쓴 회사들과 결이 비슷하고, 신입·비수도권 문이 없다 | 대기 셋을 소화한 뒤 — **금융 도메인을 처음 다룰 때** |
| 네이버클라우드 | **신입 표기가 13건**으로 가장 많지만 상당수가 **체험형 인턴·기획·영업**이다. 개발 자리는 `클라우드 인프라 보안 아키텍트`·`Foundation Model` 등 몇 건 | 개발 직군 신입 공고 비중이 늘면 |
| 포티투닷 (42dot) | **다시 세어 보니 20건 본문 있음**이고 백엔드 자리도 있다(`Backend Engineer(Software Update)`·`Senior Backend Engineer`). 다만 **5년↑이 절반**이고 여전히 안드로이드·IVI·임베디드가 중심이다 | **3~5년 백엔드 공고가 늘면** — 이전 판단보다 가까워졌다 |
| 쿠팡 | **43건 전부 본문 있음**으로 늘었고 `경력무관` 4건·`1-7년` 2건이 생겼다. 그러나 **여전히 Staff/Principal·팀장급이 다수**다 | 주니어~미들 공고가 더 늘면 |
| CJ올리브영 | 30건 전부 본문 있으나 **`경력 5년 이상`이 15건**으로 절반이다 (재확인) | 백엔드 3~5년 공고가 열리면 |
| 엑스에이아이 (xAI) | **40건 중 39건이 `신입 이상` 인데 대부분 AI Tutor(비개발)** 다. 개발은 `Mobile iOS Engineer` 정도 (재확인) | 엔지니어 공고 비중이 늘면 |
| LG유플러스 | **모집중이 1건으로 줄었고 본문이 없다** (재확인) | 개발 공고 본문이 채워지면 |
| 비상교육 | 15건 중 **본문 있는 것이 4건**이고 전부 `7년↑~10-19년` 시니어다(DevOps·GPU 최적화·Backend) | 중급 공고 본문이 채워지면 |


## 재시도 안 함

| 회사 | 두 번 찾아 두 번 다 없었던 것 |
|---|---|
| (주)사람인에이치에스 | **채용 대행사다.** 37건이 전부 남의 회사 자리(나주 우편사업진흥원, 송파 스타트업, 화성 반도체…)라 "회사 브리핑" 단위가 성립하지 않는다. 연봉·인물·도메인이 공고마다 다른 회사 것이 된다 |
| (주)제이투이 | SI 파견 중개다. 30건이 전부 고객사 프로젝트(Databricks SI, 삼성 모델러, MTS 프론트)이고 경력도 6~9년↑ |
